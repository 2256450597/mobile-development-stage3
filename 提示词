请调用你的 `ui-ux-pro-max` 技能，帮我改造 Tasty Meal Planner App 的头部和底部导航栏。

**目标效果参考**：类似 https://tasty.co 网站左上角的 “Tasty” 文字 logo 样式，以及网站顶部栏（Tips & Tricks、Products 等那一行）的浅蓝色背景。

**具体要求**：

1. **App Logo 区域（左上角）**
   - 文字内容：**“Tasty Meal Planner”**（也可以缩写为 “TMP”，但最好用全称或 “Tasty” —— 你来决定哪个更美观）
   - 字体：参考 tasty.co 的 **粗体、无衬线、略紧凑**（例如 `Poppins Bold` 或 `Inter Bold`，字重 700，大小 20px～24px）
   - 颜色：深灰色或黑色（不要纯黑），保持高级感
   - **点击行为**：点击 Logo 区域应该**重置到主页**（相当于 navigation.navigate('Home') 或类似的路由跳转）
   - 位置：固定在顶部左侧，与右侧可能的功能图标（如搜索、设置）对齐

2. **底部导航栏（Tab Bar）**
   - 当前底部有 5 个选项：**Home, Recipes, Meal Plan, Shopping, Settings**
   - **背景颜色**：改成**浅蓝色**（参考 tasty.co 网站顶部栏的背景色，类似 `#E6F0FA` 或 `#D9E8F5`，你可以推荐更合适的色值）
   - 激活状态：高亮图标/文字颜色可以加深或使用品牌橙色（`#FF8C42`）
   - 未激活状态：灰色
   - 保持圆角、内边距，适配移动端（手指友好）

3. **设计风格延续**
   - 整体背景不要纯白，建议用 `#FEF7F0` 或 `#FDF8F2`（温暖米白）
   - 头部和底部增加微阴影，与内容区分
   - 确保 Logo 点击返回主页的逻辑在生成的代码中实现（如果是前端代码，请给出路由示例；如果是设计稿，请说明交互）